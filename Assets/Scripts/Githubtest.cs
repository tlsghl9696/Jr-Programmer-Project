using UnityEngine;

public class Githubtest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("±êÇé Å×½ºÆ® Áß");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
